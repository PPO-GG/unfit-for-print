# shamefully-hoist moved to pnpm-workspace.yaml as `shamefullyHoist: true`.
# pnpm 12 does not read it here any more, and unlike the package.json `pnpm`
# field it does not warn either -- it just stops hoisting. Leaving the old
# line in place would have read like configuration while doing nothing.
